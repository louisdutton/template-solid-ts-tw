import type { Component } from "solid-js";

const App: Component = () => {
  return (
    <main class="flex h-screen w-screen flex-col items-center justify-center">
      <p class="text py-20 text-center text-4xl">SolidJS + TS + TailwindCSS</p>
    </main>
  );
};

export default App;
